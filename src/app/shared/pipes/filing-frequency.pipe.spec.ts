import { FilingFrequencyPipe } from './filing-frequency.pipe';

describe('FilingFrequencyPipe', () => {
  it('create an instance', () => {
    const pipe = new FilingFrequencyPipe();
    expect(pipe).toBeTruthy();
  });
});
